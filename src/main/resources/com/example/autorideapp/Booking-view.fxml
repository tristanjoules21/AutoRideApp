<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane prefHeight="1080.0" prefWidth="1920.0"
            stylesheets="@booking.css"
            xmlns="http://javafx.com/javafx/17.0.12"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.autorideapp.autoride.BookingManagementController">

    <!-- Sidebar -->
    <left>
        <VBox prefWidth="280.0" spacing="30" styleClass="sidebar">
            <padding>
                <Insets bottom="30" left="30" right="30" top="40" />
            </padding>

            <!-- Header -->
            <VBox spacing="4">
                <Label styleClass="brand" text="AutoRide" />
                <Label styleClass="subtitle" text="Car Rental System" />
            </VBox>

            <!-- Navigation -->
            <VBox spacing="12" VBox.vgrow="ALWAYS">
                <Button styleClass="nav-item" text="🏠 Dashboard"
                        onAction="#showDashboardView" />
                <Button styleClass="nav-item" text="🚗 Car Management"
                        onAction="#showCarManagementView" />
                <Button styleClass="nav-item-active" text="📅 Booking Management"
                        onAction="#showBookingView" />
                <Button styleClass="nav-item" text="👥 Customer Management"
                        onAction="#showCustomerManagementView" />
                <Button styleClass="nav-item" text="👤 User Management"
                        onAction="#showUserManagementView" />
            </VBox>

            <!-- Admin Info + Logout -->
            <VBox spacing="15">
                <VBox alignment="CENTER" spacing="3">
                    <Label styleClass="admin-name" text="Admin User" />
                    <Label styleClass="admin-email" text="admin@autoride.com" />
                </VBox>
                <Button onAction="#handleLogout"
                        styleClass="logout-btn" text="⏻ Logout" />
            </VBox>
        </VBox>
    </left>

    <!-- Main Content -->
    <center>
        <VBox spacing="25" styleClass="main-content">
            <padding>
                <Insets bottom="40" left="50" right="50" top="40" />
            </padding>

            <!-- Header -->
            <HBox alignment="CENTER_LEFT" spacing="15">
                <Label fx:id="mainTitle" styleClass="main-title"
                       text="Booking Management" />
                <Pane HBox.hgrow="ALWAYS" />
                <TextField fx:id="searchField"
                           promptText="🔍 Search bookings..."
                           prefWidth="300.0"
                           styleClass="search-field" />
                <Button onAction="#handleAddBooking"
                        styleClass="add-btn" text="+ New Booking" />
            </HBox>

            <!-- Booking Table -->
            <TableView fx:id="bookingTable" prefHeight="700.0" styleClass="booking-table">
                <columns>
                    <TableColumn prefWidth="150.0" text="Booking ID" />
                    <TableColumn prefWidth="250.0" text="Customer" />
                    <TableColumn prefWidth="250.0" text="Car" />
                    <TableColumn prefWidth="300.0" text="Dates" />
                    <TableColumn prefWidth="200.0" text="Amount" />
                    <TableColumn prefWidth="200.0" text="Payment" />
                    <TableColumn prefWidth="150.0" text="Actions" />
                </columns>
            </TableView>
        </VBox>
    </center>
</BorderPane>
